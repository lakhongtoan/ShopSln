@model List<Shop.Models.CartItem>
@{
    ViewData["Title"] = "Shopping Cart";
}

<section id="cart_items">
    <div class="container">
        <div class="breadcrumbs">
            <ol class="breadcrumb">
                <li><a href="#">Home</a></li>
                <li class="active">Giỏ hàng</li>
            </ol>
        </div>
        <div class="table-responsive cart_info">
            <table class="table table-condensed">
                <thead>
                    <tr class="cart_menu">
                        <td class="image">Sản phẩm</td>
                        <td class="description"></td>
                        <td class="price">Giá</td>
                        <td class="quantity">Số lượng</td>
                        <td class="total">Thành tiền</td>
                        <td></td>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="cart_product">
                                    <a href=""><img src="@(item.ProductImage ?? "/images/home/anh.jpg")" alt="" style="width: 100px; height: 100px;"></a>
                                </td>
                                <td class="cart_description">
                                    <h4><a href="">@item.ProductName</a></h4>
                                    <p>Mã SP: @item.ProductId</p>
                                </td>
                                <td class="cart_price">
                                    <p>$@item.UnitPrice</p>
                                </td>
                                <td class="cart_quantity">
                                    <div class="cart_quantity_button">
                                        <a class="cart_quantity_up" href="#" data-cart-item-id="@item.CartItemId"> + </a>
                                        <input class="cart_quantity_input" type="text" name="quantity" value="@item.Quantity" autocomplete="off" size="2">
                                        <a class="cart_quantity_down" href="#" data-cart-item-id="@item.CartItemId"> - </a>
                                    </div>
                                </td>
                                <td class="cart_total">
                                    <p class="cart_total_price">$@item.TotalPrice</p>
                                </td>
                                <td class="cart_delete">
                                    <a class="cart_quantity_delete" href="#" data-cart-item-id="@item.CartItemId"><i class="fa fa-times"></i></a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="6" class="text-center">
                                <p>Giỏ hàng của bạn đang trống</p>
                                <a href="@Url.Action("Shop", "Shop")" class="btn btn-default">Tiếp tục mua sắm</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</section>
<!--/#cart_items-->

@if (Model != null && Model.Any())
{
    <section id="do_action">
        <div class="container">
            <div class="heading">
                <h3>Bạn muốn làm gì tiếp theo?</h3>
                <p>Chọn nếu bạn có mã giảm giá hoặc điểm thưởng muốn sử dụng hoặc muốn ước tính chi phí vận chuyển.</p>
            </div>
            <div class="row">
                <div class="col-sm-6">
                    <div class="chose_area">
                        <ul class="user_option">
                            <li>
                                <input type="checkbox">
                                <label>Sử dụng mã giảm giá</label>
                            </li>
                            <li>
                                <input type="checkbox">
                                <label>Sử dụng phiếu quà tặng</label>
                            </li>
                            <li>
                                <input type="checkbox">
                                <label>Ước tính phí vận chuyển & thuế</label>
                            </li>
                        </ul>
                        <ul class="user_info">
                            <li class="single_field">
                                <label>Quốc gia:</label>
                                <select>
                                    <option>United States</option>
                                    <option>Bangladesh</option>
                                    <option>UK</option>
                                    <option>India</option>
                                    <option>Pakistan</option>
                                    <option>Ucrane</option>
                                    <option>Canada</option>
                                    <option>Dubai</option>
                                </select>

                            </li>
                            <li class="single_field">
                                <label>Tỉnh / Thành phố:</label>
                                <select>
                                    <option>Select</option>
                                    <option>Dhaka</option>
                                    <option>London</option>
                                    <option>Dillih</option>
                                    <option>Lahore</option>
                                    <option>Alaska</option>
                                    <option>Canada</option>
                                    <option>Dubai</option>
                                </select>

                            </li>
                            <li class="single_field zip-field">
                                <label>Mã bưu điện:</label>
                                <input type="text">
                            </li>
                        </ul>
                        <a class="btn btn-default update" href="">Lấy báo giá</a>
                        <a class="btn btn-default check_out" href="">Tiếp tục</a>
                    </div>
                </div>
                <div class="col-sm-6">
                    <div class="total_area">
                        <ul>
                            <li>Tạm tính <span>$@Model.Sum(x => x.TotalPrice)</span></li>
                            <li>Thuế <span>$2</span></li>
                            <li>Phí vận chuyển <span>Miễn phí</span></li>
                            <li>Tổng cộng <span>$@(Model.Sum(x => x.TotalPrice) + 2)</span></li>
                        </ul>
                        <a class="btn btn-default update" href="">Cập nhật</a>
                        <a class="btn btn-default check_out" href="@Url.Action("Checkout", "Shop")">Thanh toán</a>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!--/#do_action-->
}

@section Scripts {
    <script>
        $(document).ready(function() {
            $('.cart_quantity_up').click(function(e) {
                e.preventDefault();
                var cartItemId = $(this).data('cart-item-id');
                var currentQty = parseInt($(this).siblings('.cart_quantity_input').val());
                updateCartItem(cartItemId, currentQty + 1);
            });

            $('.cart_quantity_down').click(function(e) {
                e.preventDefault();
                var cartItemId = $(this).data('cart-item-id');
                var currentQty = parseInt($(this).siblings('.cart_quantity_input').val());
                if (currentQty > 1) {
                    updateCartItem(cartItemId, currentQty - 1);
                }
            });

            $('.cart_quantity_delete').click(function(e) {
                e.preventDefault();
                var cartItemId = $(this).data('cart-item-id');
                removeFromCart(cartItemId);
            });

            function updateCartItem(cartItemId, quantity) {
                $.ajax({
                    url: '@Url.Action("UpdateCartItem", "Shop")',
                    type: 'POST',
                    data: { cartItemId: cartItemId, quantity: quantity },
                    success: function(response) {
                        if (response.success) {
                            location.reload();
                        } else {
                            alert('Lỗi khi cập nhật giỏ hàng');
                        }
                    },
                    error: function() {
                        alert('Error updating cart');
                    }
                });
            }

            function removeFromCart(cartItemId) {
                $.ajax({
                    url: '@Url.Action("RemoveFromCart", "Shop")',
                    type: 'POST',
                    data: { cartItemId: cartItemId },
                    success: function(response) {
                        if (response.success) {
                            location.reload();
                        } else {
                            alert('Lỗi khi xóa sản phẩm khỏi giỏ hàng');
                        }
                    },
                    error: function() {
                        alert('Error removing item from cart');
                    }
                });
            }
        });
    </script>
}
